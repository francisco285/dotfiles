" Use color as default app for pickachu (options: color, date, file)
let g:pickachu_default_app = "color"
" Use hexa as default format (options: hex, rgb, rgba)
let g:pickachu_default_color_format = "hex"
